name = "rcs"
main = "src/worker.js"
compatibility_date = "2024-01-01"

[assets]
directory = "./dist"
binding = "ASSETS"

# The Worker script at src/worker.js handles SPA routing.
# No _redirects file is needed as it can cause loop errors during Worker builds.
